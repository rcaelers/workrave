//! Release notes: `changes.yaml` rendered through the templates in
//! `tools/ship/templates` (NEWS, GitHub, blog, Debian changelog, appcast).

pub mod markdown_text;
pub mod newsgen;
pub mod templates;
